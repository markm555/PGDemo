using System;
using System.Collections.Generic;
using System.Text;

namespace PGDemo
{
    class CreateTable
    {

        /* Create a new KDFW table

        var version = cmd.ExecuteScalar().ToString();
;
        cmd.Connection = con;

        cmd.CommandText = "cmd.ExecuteNonQuery();";
        cmd.ExecuteNonQuery();

        cmd.CommandText = @"CREATE TABLE KDFW(id SERIAL PRIMARY KEY,
                dt timestamp, flight VARCHAR(255), gs INT, baro_rate INT, nav_mode VARCHAR(255), lat INT, lon INT)";
        cmd.ExecuteNonQuery();

        //cmd.CommandText = "INSERT INTO cars(name, price) VALUES('Audi',52642)";
        //cmd.ExecuteNonQuery();



        Console.WriteLine("Table KDFW created");

        */
    }
}

